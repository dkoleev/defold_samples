local manager = require("src.game_manager")

function init(self)
	msg.post(".", "acquire_input_focus")
	print("INIT")
end

function on_input(self, action_id, action)
	manager.on_input(action_id, action)
end
